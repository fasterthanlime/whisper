[package]
name = "bee-vad"
version = "0.1.0"
edition = "2021"
description = "Silero VAD v5 voice activity detection via MLX on Apple Silicon"
license = "MIT"

[dependencies]
mlx-rs.workspace = true
log = "0.4"
