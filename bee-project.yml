name: bee
options:
  bundleIdPrefix: fasterthanlime
  deploymentTarget:
    macOS: "15.0"
  xcodeVersion: "16.3"
  defaultConfig: Release

settings:
  base:
    CLANG_CXX_LANGUAGE_STANDARD: "gnu++20"
    GCC_C_LANGUAGE_STANDARD: gnu17
    DEVELOPMENT_TEAM: B2N6FSRTPV
    ENABLE_USER_SCRIPT_SANDBOXING: YES

targets:
  bee:
    type: application
    platform: macOS
    sources:
      - path: bee
        excludes:
          - bee-Bridging-Header.h
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: fasterthanlime.bee
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_LSUIElement: YES
        INFOPLIST_KEY_ATSApplicationFontsPath: Fonts
        INFOPLIST_KEY_NSMicrophoneUsageDescription: "Bee needs microphone access for voice-to-text dictation."
        SWIFT_VERSION: "6.0"
        SWIFT_OBJC_BRIDGING_HEADER: bee/bee-Bridging-Header.h
        SWIFT_APPROACHABLE_CONCURRENCY: YES
        SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated
        SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY: YES
        ENABLE_APP_SANDBOX: NO
        ENABLE_HARDENED_RUNTIME: YES
        HEADER_SEARCH_PATHS: "$(PROJECT_DIR)/qwen3-asr-mlx-rs/qwen3-asr-mlx-ffi/include"
        LIBRARY_SEARCH_PATHS: "$(PROJECT_DIR)/qwen3-asr-mlx-rs/target/release"
        OTHER_LDFLAGS:
          - "-lqwen3_asr_mlx_ffi"
          - "-lc++"
          - "-framework"
          - Metal
          - "-framework"
          - MetalPerformanceShaders
          - "-framework"
          - Accelerate
          - "-framework"
          - Security
          - "-framework"
          - SystemConfiguration
        CODE_SIGN_ENTITLEMENTS: bee/bee.entitlements
    entitlements:
      path: bee/bee.entitlements
      properties:
        com.apple.security.device.audio-input: true

  beeInput:
    type: application
    platform: macOS
    sources:
      - path: bee-input
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: fasterthanlime.inputmethod.bee
        PRODUCT_NAME: beeInput
        INFOPLIST_FILE: bee-input/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        SWIFT_VERSION: "5.0"
        SWIFT_APPROACHABLE_CONCURRENCY: YES
        SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
        SWIFT_UPCOMING_FEATURE_MEMBER_IMPORT_VISIBILITY: YES
        ENABLE_APP_SANDBOX: NO
        ENABLE_HARDENED_RUNTIME: YES
    dependencies:
      - sdk: InputMethodKit.framework
