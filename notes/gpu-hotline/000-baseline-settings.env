# Fixed streaming settings for GPU hotline baseline runs.
# These start from current app defaults rather than transient slider state.

BEE_CHUNK_DURATION=0.5
BEE_VAD_THRESHOLD=0.5
BEE_ROLLBACK_TOKENS=5
BEE_COMMIT_TOKENS=12
BEE_MAX_TOKENS_STREAMING=32
BEE_MAX_TOKENS_FINAL=512
BEE_DISABLE_CORRECTION=1
